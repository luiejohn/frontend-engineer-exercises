import Layout from '@components/Layout';
import Login from '@components/login/login';
import { FC } from 'react';

const Home: FC = () => (
    <Layout>
      <Login />
    </Layout>
);

export default Home;
